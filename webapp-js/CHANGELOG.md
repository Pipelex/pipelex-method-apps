# Changelog

This template is released as part of the `pipelex-method-apps` family, which carries one version for every template, so its history is the family's changelog at the root of that repository.

A project created from this directory starts its own history here: the bootstrap replaces this file with the project's first entry.
