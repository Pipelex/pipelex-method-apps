node_modules/
.next/
out/
build/
dist/
coverage/
package-lock.json
*.tgz

# Generated by `npm run codegen` — written verbatim from the API's artifacts.
# The ts-zod emitter targets Prettier's DEFAULTS (80 cols); this repo prints at
# 100, so formatting these would rewrite bytes, break every stamp, and make
# `npm run codegen:check` report the whole tree as hand-edited.
src/generated/

# Machine-local caches written by tooling. A global gitignore can hide them from
# git, but Prettier reads only this directory's `.gitignore` and
# `.prettierignore`, so an unformatted one would fail `make format-check` on a
# tree that is otherwise clean.
.gstack/
