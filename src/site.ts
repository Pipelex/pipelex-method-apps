/**
 * The app's identity, in one place: the page heading, the browser title and the
 * meta description all read it, so renaming the app is an edit to two strings.
 *
 * The bootstrap rewrites both values as string literals — never as JSX text.
 * Edit them freely afterwards.
 */
export const SITE = {
  title: "Pipelex Method App",
  description:
    "A Next.js app that runs MTHDS methods through the Pipelex API, with each method's input form and result view generated from its own contract.",
};
