# Rendered by `make workflows` from webapp-js/.github/workflows/tests-check.yml — edit that file and re-render.
# GitHub reads workflows only at the repository root, and the template's own copy travels
# into every project created from it, so the root runs this twin inside the template.
name: Tests check (webapp-js)

on:
  pull_request:

jobs:
  tests:
    name: Tests (webapp-js)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: webapp-js
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: webapp-js/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run unit tests
        run: make agent-test

      - name: Build
        run: make build
